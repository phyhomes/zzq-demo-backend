<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.zzq.system.mapper.SysConfigMapper">
    <resultMap id="SysConfigResult" type="SysConfig">
        <id column="config_id" property="id" />
        <result column="name" property="name" />
        <result column="config_key" property="configKey" />
        <result column="config_value" property="configValue" />
        <result column="type" property="type" />
        <result column="create_by" property="createBy" />
        <result column="create_time" property="createTime" />
        <result column="update_by" property="updateBy" />
        <result column="update_time" property="updateTime" />
        <result column="remark" property="remark" />
    </resultMap>
    <resultMap id="SysConfigVoResult" type="SysConfigVO">
        <id column="config_id" property="id" />
        <result column="name" property="name" />
        <result column="config_key" property="configKey" />
        <result column="config_value" property="configValue" />
    </resultMap>
    <select id="selectConfigByIdOrKey" resultMap="SysConfigResult">
        select id as config_id, name, config_key, config_value, type, remark
        from sys_config
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="configKey != null and configKey != ''">
                and config_key = #{configKey}
            </if>
        </where>
    </select>
    <select id="listSysConfig" resultMap="SysConfigVoResult">
        select c.id as config_id, c.name, c.config_key, c.config_value, c.remark
        from sys_config c
        <where>
            <if test="name != null and name != ''">
                and c.name like concat('%', #{name}, '%')
            </if>
            <if test="configKey != null and configKey != ''">
                and c.config_key like concat('%', #{configKey}, '%')
            </if>
            <if test="configValue != null and configValue != ''">
                and c.config_value like concat('%', #{configValue}, '%')
            </if>
        </where>
    </select>


</mapper>